name: Maven Build & Test

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: 
          - name: order-service
            path: order-microservice-orchestration-pattern/order-service
          - name: inventory-service
            path: order-microservice-orchestration-pattern/inventory-service
          - name: payment-service
            path: order-microservice-orchestration-pattern/payment-service
          - name: shipment-service
            path: order-microservice-orchestration-pattern/shipment-service

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🗄️ Setup JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'maven'

    - name: 🧪 Test ${{ matrix.service.name }}
      working-directory: ${{ matrix.service.path }}
      run: mvn -B test

    - name: 📦 Build ${{ matrix.service.name }}
      working-directory: ${{ matrix.service.path }}
      run: mvn -B package -DskipTests
